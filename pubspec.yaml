name: project_management_app
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0' # Ensure this matches your Flutter SDK version from 'flutter doctor'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  # Add the graphql_flutter package here
  graphql_flutter: ^5.0.0 # Use the latest stable version

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
